

Add all the theories for the following question in detail and wherever possible add diagram and add code






Explain Authorization Grant flow with spring Security with code and diagram and how to configure it? Use the following step
  - Third Party App registration to the Authorization server(Google/Gmail/Github/Gitlab/Auth0), also add the redirect URL/Callback Url and set the scopes for this App and retrieves the client id and client secret and
  - import spring boot starter oauth2 client. There are already client code written for multiple Authoriation server like google, github, gitlab, Auth0 in that dependency, For these we don't need to do much. We will just need to add the config in the application.yaml/properties We can add configurations for more than one providers. (Show all the configs possible for the popular providers and their meaning)
  - Update the security filter chain with `oauth2Login(Customizers.withDefault())` 
  - When /login invoked then internally `DefaultLoginPageGenerating.java` is invoked and inside that the html page is generated and returned.so if we visit localhost:8080, it will show us the list of providers that we have configured in the config. 
  - Once we click one of them, it will redirect to the `localhost:8080/oauth2/authorization/{registration_id}` and `OAuth2AuthorizationRequestRedirectFilter` invoked, then it will check the config and then it will redirect to the authorize endpoint of the authorization server, which will redirected into the login url and After the Successfull login is done then authorization server will redirect the user to registered redirect URL/Callback Url along with authorization code and state(optional but highly recomended)

  - We can choose one of them and it will redirect to the login page of the provider/resource server, we need to provide the user credential and after successfull login it will return the authrization code and then the library code will call the token endpoint internaly with cliwnt id, secret and auth code. 
  - So most of the code for popular vendor is already present in the library code, we just need add the configuration, and Security filter chain code that's it
  - Spring boot by default assumes that the OAuth2 login will be done on a browser, so by default it creates a session in browser, By default spring security make it statefull, however we could make it stateless



- Explain Client credential flow with spring Security with code and diagram and diagram and how to configure it


- Explain PKCE with spring Security with code and diagram and diagram and how to configure it