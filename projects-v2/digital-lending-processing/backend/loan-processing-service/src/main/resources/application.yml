spring:
  application:
    name: loan-processing-service
  threads:
    virtual:
      enabled: true
  data:

jwt:
  secret: loansphere-docker-secret-key-must-be-at-least-256-bits-long
  expiration-ms: 3600000

server:
  port: 8086

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/

resilience4j:
  circuitbreaker:
    instances:
      applicationService:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10000
  retry:
    instances:
      applicationService:
        max-attempts: 3
        wait-duration: 500ms
management.endpoint.health.show-details: always
management:
  server:
    port: 9087
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health
