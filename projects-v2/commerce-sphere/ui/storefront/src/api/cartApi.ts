import client from './client'
export const cartApi = { get: (id: string) => client.get(`/api/cart/${id}`).then(r => r.data), addItem: (cartId: string, item: any) => client.post(`/api/cart/${cartId}/items`, item).then(r => r.data), removeItem: (cartId: string, pid: string) => client.delete(`/api/cart/${cartId}/items/${pid}`).then(r => r.data), updateQty: (cartId: string, pid: string, qty: number) => client.put(`/api/cart/${cartId}/items/${pid}?quantity=${qty}`).then(r => r.data), merge: (guestId: string, userId: string) => client.post('/api/cart/merge', { guestId, userId }).then(r => r.data) }
export const wishlistApi = { get: (uid: string) => client.get(`/api/cart/wishlist/${uid}`).then(r => r.data), add: (uid: string, item: any) => client.post(`/api/cart/wishlist/${uid}/items`, item), remove: (uid: string, pid: string) => client.delete(`/api/cart/wishlist/${uid}/items/${pid}`) }
